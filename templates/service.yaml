apiVersion: v1
kind: Service
metadata:
  name: {{ include "jimmerioles-weather-app.fullname" . }}
  labels:
    {{- include "jimmerioles-weather-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jimmerioles-weather-app.selectorLabels" . | nindent 4 }}
